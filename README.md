# Meditation API (Node + Express + Prisma + MySQL)

## Quick start
```bash
cp .env.example .env
docker compose up -d db
npm install
npm run prisma:generate
npm run prisma:migrate
npm run seed
npm run dev
```
